# MyLeafy 发布记录

本文是 MyLeafy 正式版本、源码锚点和用户可见更新摘要的唯一记录入口。App Store 实际使用过的短文案与根据 Git 历史补录的版本摘要分开标注；特定审核过程见 [`app-store/`](app-store/)。正式 tag 创建后不得移动、覆盖或强制更新。

发布通过 GitHub Actions 的 `Cut iOS Release` workflow（`workflow_dispatch`）完成：它会用当前 `main` 构建未签名的 `.xcarchive`，在 `main` 上创建 `vX.Y` tag，并生成一个挂载归档的 draft Release。确认草稿内容后手动发布即可；App Store 提交流程仍为人工操作。

## 未发布

- 根导航为课表、社区、日迹、校园、我的；日迹顶部提供随记、日程、推送。
- 课表按单个学年浏览，学校单学期课表保留 20 周数据范围；随记和个人日程按校园身份保存在本地。
- 日迹卡片使用白色系统表面，Tag 使用白字主题色胶囊；筛选显示当前 Tag 并提供“全部随记”，评分详情星级居中。
- 日迹输入器支持聚焦展开、逐行增高和长文本放大编辑；加号菜单不再提供新建文章入口，历史文章仍可查看和编辑。
- “记录”分组新增“记录日迹”，提供自然年、近 30 天、连续记录、习惯与里程碑统计，并可在本机生成只含聚合数据的分享图片。
- 校园一级领域“空闲教室”调整为“自习安排”，专注记录移入“学习空间”；编辑资料和“我的”主页隐藏竖向滚动指示器。
- 教务同步现在区分完整成功、部分成功和失败；异常或无法识别的空页面不会覆盖最近成功缓存。
- 改进系统大字体、英文界面、记录日迹图表读屏、社区局部错误恢复和发布重试可靠性。
- 课表双指缩放改为同一日期列布局内的可逆连续形变：本周让今天居中，其他周从周一至周三进入；三日视图支持每次三个自然日的左右分页并缩回当前页面所属周。时间视图年度缩略恢复 1–12 月自然顺序，照片背景替换后立即刷新并默认完整显示。
- 课程教师可直达评价页；投票卡片无需先投票即可查看各选项比例；阳光长跑采用 2026–2027 第一学期日期和假期周。
- 新建个人日程会以同一条记录同时显示在日程列表和随记流，并按真实创建时间参与随记搜索与排序。

## 2.9

| 项目 | 内容 |
|---|---|
| 状态 | 已正式发布 |
| App Store 发布时间 | 2026-08-03 |
| Build | 27 |
| Git tag | `v2.9` |
| 发布源码 | `6d5c0331266b49461153fb47b9a17a1f37e5a230` |

App Store 实际更新说明：

```text
为新学期做准备。
```

根据 Git 历史补录的版本摘要：

- 统一考试、自定日程、倒计时与提醒的时间表达，并完善课表背景和共享课表体验。
- 扩展社区附件、评论、草稿、发布任务与本地分享卡片，提升内容发布和恢复能力。
- 完善账户永久删除、审核演示账号和关键异常反馈，修复审核阶段发现的稳定性问题。

## 2.8

| 项目 | 内容 |
|---|---|
| 状态 | 历史正式版本 |
| 源码定版日期 | 2026-07-13 |
| Build | 19 |
| Git tag | `v2.8` |
| 发布源码 | `716cb3e50f4cf73a3d95ef82959f3091106d60a1` |

根据 Git 历史补录的版本摘要：

- 根导航迁移到原生系统 Tab，并优化课表快捷入口、天气提示和校园页面细节。

- 加强邮箱验证、认证链路、运营后台和数据库操作的安全性与可靠性。

## 1.9

App Store 更新说明：

```text
MyLeafy 1.9 优化校园功能流程和多语言体验。

学习项目现在支持删除，并可选择保留资料、任务和学习记录到“其他”，或连同项目内容一起清理。学业与职业模块的行操作也更紧凑，减少误触和视觉占用。

本次还调整了共享课表、荣誉记录和小组件的本地化表达，补充繁体中文文案，并将英文品牌字体改为 Lora。
```

## 1.6

App Store 更新说明：

```text
MyLeafy 1.6 调整了社区信息层级，并优化了校园功能的操作流程。

置顶内容现在会在社区中更醒目地显示。我们也优化了帖子流、反馈、评教和共享课表的细节体验，并提升了整体稳定性。
```
